# Copy to .env.test and fill in values for Playwright E2E tests
# Run: npm run test:e2e  (loads this file automatically, or set env vars in shell)
#
# Test user credentials (must exist in Supabase)
# IMPORTANT: Use the correct email - e.g. .com not .comm
TEST_USER_EMAIL=hassanonclouds@gmail.com    
TEST_USER_PASSWORD=123456789

# Optional: override base URL (default: http://localhost:3000)
PLAYWRIGHT_BASE_URL=http://localhost:3000